================ ADDITIONS TO INDEX.HTML ================

1. ADD NEW BUTTONS TO INVESTORS ADMIN TOOLBAR (around line 989-1002):
After the existing buttons, add:

            <button onclick="viewProfitDistributionReport()" class="px-4 py-2.5 bg-emerald-50 border border-emerald-200 hover:bg-emerald-100 text-emerald-700 rounded-xl font-semibold text-sm shadow-sm transition-all duration-200 flex items-center gap-2">
              <i class="ph ph-chart-pie-slice"></i>
              <span>توزيع الأرباح المحصلة</span>
            </button>
            <button onclick="viewProfitComparisonReport()" class="px-4 py-2.5 bg-sky-50 border border-sky-200 hover:bg-sky-100 text-sky-700 rounded-xl font-semibold text-sm shadow-sm transition-all duration-200 flex items-center gap-2">
              <i class="ph ph-chart-bar"></i>
              <span>تقرير مقارن</span>
            </button>

---

2. ADD NEW NAVIGATION LINK IN SIDEBAR (around line 191-194, after expenses):
Add this new tab link:

      <a href="#" data-tab="today-reminders" class="flex items-center gap-3 px-3 md:px-4 py-2.5 md:py-3 rounded-xl text-slate-300 hover:bg-skyDark-800 hover:text-white font-medium transition-all duration-200">
        <i class="ph ph-bell-ringing text-base md:text-lg w-5 shrink-0"></i>
        <span>تنبيهات اليوم</span>
      </a>

---

3. ADD NEW TAB CONTENT SECTION (around line 1071, before the Reports tab):
Add this new section:

      <!-- ================= TAB: TODAY DUE REMINDERS ================= -->
      <section id="tab-today-reminders" class="space-y-6 hidden">
        <div class="flex items-center justify-between flex-wrap gap-3">
          <div>
            <h3 class="text-xl font-bold text-slate-800">تنبيهات الأقساط المستحقة اليوم</h3>
            <p class="text-sm text-slate-500">عرض الأقساط المستحقة اليوم وإرسال تنبيهات جماعية بضغطة زر واحدة.</p>
          </div>
          <button onclick="sendTodayDueRemindersInBulk()" class="px-5 py-2.5 bg-teal-600 hover:bg-teal-700 text-white rounded-xl font-semibold text-sm shadow-lg shadow-teal-600/20 transition-all duration-200 flex items-center gap-2">
            <i class="ph ph-paper-plane"></i>
            <span>إرسال تنبيهات جماعية الآن</span>
          </button>
        </div>
        
        <!-- Today's Due Summary -->
        <div id="today-due-summary" class="grid grid-cols-2 lg:grid-cols-4 gap-2.5 sm:gap-6">
          <!-- Populated dynamically by JavaScript -->
        </div>
        
        <!-- Today's Installments List -->
        <div class="bg-white rounded-2xl border border-slate-100 shadow-sm overflow-hidden p-3 sm:p-6 space-y-3 sm:space-y-4">
          <h4 class="font-bold text-slate-800 text-sm sm:text-lg border-b border-slate-100 pb-2 sm:pb-3">تفاصيل الأقساط المستحقة اليوم</h4>
          
          <div class="table-scroll-wrapper">
            <table class="w-full text-right border-collapse">
              <thead>
                <tr class="bg-slate-50 border-b border-slate-100 text-slate-600 text-sm font-semibold">
                  <th class="p-4">اسم العميل</th>
                  <th class="p-4">رقم الهاتف</th>
                  <th class="p-4">المبلغ</th>
                  <th class="p-4">الحالة</th>
                  <th class="p-4 text-center">الإجراءات</th>
                </tr>
              </thead>
              <tbody id="today-installments-body" class="text-sm divide-y divide-slate-100">
                <!-- Populated dynamically -->
              </tbody>
            </table>
          </div>
          
          <div id="today-empty-state" class="p-6 text-center text-slate-400 text-sm">
            لا توجد أقساط مستحقة اليوم
          </div>
        </div>
      </section>

---

4. UPDATE STAFF PERMISSIONS (in app.js around line 75):
Add 'today-reminders' to the STAFF_PERMISSION_TABS array:

const STAFF_PERMISSION_TABS = ['clients', 'client-balances', 'inventory', 'suppliers', 'products', 'contracts', 'collections', 'treasury', 'investors', 'expenses', 'today-reminders', 'reports'];

---

5. UPDATE STAFF PERMISSIONS IN DEFAULT SETTINGS (in app.js around line 139):
Add to staffPermissions object:

      'today-reminders': true,

---

6. UPDATE SWITCH TAB FUNCTION (in app.js, add case for new tab):
Add this case in the switchTab function:

    case 'today-reminders':
      renderTodayReminders();
      break;

---

7. ADD NEW FUNCTION TO APP.JS:
Add this function to render the today reminders tab:

function renderTodayReminders() {
  const stats = getTodayDueStats();
  
  // Render summary cards
  const summaryContainer = document.getElementById('today-due-summary');
  summaryContainer.innerHTML = `
    <div class="bg-amber-900 text-white rounded-xl sm:rounded-2xl p-3 sm:p-6 shadow-xl relative overflow-hidden flex flex-col justify-between h-20 sm:h-32">
      <p class="text-[10px] sm:text-xs text-amber-200 font-semibold truncate">الأقساط المستحقة اليوم</p>
      <h4 class="text-base sm:text-3xl font-extrabold mt-1.5 sm:mt-3 text-amber-300 truncate">${stats.totalCount}</h4>
    </div>
    <div class="bg-slate-900 text-white rounded-xl sm:rounded-2xl p-3 sm:p-6 shadow-xl relative overflow-hidden flex flex-col justify-between h-20 sm:h-32">
      <p class="text-[10px] sm:text-xs text-slate-400 font-semibold truncate">الإجمالي المطلوب</p>
      <h4 class="text-base sm:text-3xl font-extrabold mt-1.5 sm:mt-3 text-teal-400 truncate">${stats.totalDueAmount.toLocaleString()} ج.م</h4>
    </div>
    <div class="bg-rose-900 text-white rounded-xl sm:rounded-2xl p-3 sm:p-6 shadow-xl relative overflow-hidden flex flex-col justify-between h-20 sm:h-32">
      <p class="text-[10px] sm:text-xs text-rose-200 font-semibold truncate">متأخرة</p>
      <h4 class="text-base sm:text-3xl font-extrabold mt-1.5 sm:mt-3 text-rose-300 truncate">${stats.overdueCount}</h4>
    </div>
    <div class="bg-emerald-900 text-white rounded-xl sm:rounded-2xl p-3 sm:p-6 shadow-xl relative overflow-hidden flex flex-col justify-between h-20 sm:h-32">
      <p class="text-[10px] sm:text-xs text-emerald-200 font-semibold truncate">قيد الاستحقاق</p>
      <h4 class="text-base sm:text-3xl font-extrabold mt-1.5 sm:mt-3 text-emerald-300 truncate">${stats.pendingCount}</h4>
    </div>
  `;
  
  // Render installments table
  const tbody = document.getElementById('today-installments-body');
  const emptyState = document.getElementById('today-empty-state');
  
  if (stats.totalCount === 0) {
    tbody.innerHTML = '';
    emptyState.classList.remove('hidden');
  } else {
    emptyState.classList.add('hidden');
    tbody.innerHTML = stats.installments.map(inst => {
      const status = getInstallmentOverdueStatus(inst);
      const statusText = status.overdueDays > 0 ? `⚠️ متأخر ${status.overdueDays} يوم` : '📅 يستحق اليوم';
      const statusColor = status.overdueDays > 0 ? 'text-rose-600' : 'text-sky-600';
      
      return `
        <tr class="hover:bg-slate-50">
          <td class="p-4 font-bold text-slate-800">${escapeHTML(inst.clientName)}</td>
          <td class="p-4 font-mono text-slate-600">${escapeHTML(inst.clientPhone)}</td>
          <td class="p-4 font-bold font-mono text-teal-600">${inst.amount.toLocaleString()} ج.م</td>
          <td class="p-4 text-sm ${statusColor}">${statusText}</td>
          <td class="p-4 text-center">
            <div class="inline-flex gap-1">
              <button onclick="openWhatsappModal('${inst.id}', 'reminder')" class="px-2 py-1 bg-sky-50 text-sky-700 hover:bg-sky-100 rounded text-xs font-bold transition-all">
                <i class="ph ph-bell"></i> تذكير
              </button>
              <button onclick="collectInstallmentBtn('${inst.id}')" class="px-2 py-1 bg-emerald-50 text-emerald-700 hover:bg-emerald-100 rounded text-xs font-bold transition-all">
                <i class="ph ph-check-square"></i> تحصيل
              </button>
            </div>
          </td>
        </tr>
      `;
    }).join('');
  }
}
